name: sample run

on:
  pull_request:
    branches:
      - "*"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout branch 
        uses: actions/checkout@v3
      
      - name: print 
        run: echo "Hello World!"
        